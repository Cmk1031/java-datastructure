배열은 가장 기본족인 자료 구조이고, 특히 인덱스를 사용할 때 최고의 효율이 나온다.
하지만 배열의 크기를 배열을 생성하는 시점에 미리 정해야 하는 단점이 존재한다.
(배열의 길이를 동적 변경 불가능 + 데이터를 추가하기 불편)
동적으로 언제든지 길이를 늘리고 줄이는 방법이 있을까?
-> 배열 리스트 (ArrayList!)

리스트: 순서가 있고 중복을 허용하며 크기가 동적으로 변할 수 있다.

배열 리스트는 마지막에 데이터를 추가 or 삭제할 때는 O(1)로 매우 빠르지만, 중간에 데이터를 추가하거나 삭제하는 경우에는
O(n)으로 느리다.

앞서 만든 MyArrayList들은 Object를 입력받기 때문에 아무 데이터나 입력할 수 있고, 또 결과로 Object를 반환한다.
따라서 필요한 경우 다운 캐스팅을 해야하고, 또 타입 안전성이 떨어지는 단점이 있다.
-->> 제네릭을 사용하자!
제네릭 사용시 타입 인자로 지정한 타입으로만 안전하게 데이터를 저장하고, 조회할 수 있다. 